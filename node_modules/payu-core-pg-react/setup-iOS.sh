#!/bin/bash

# This script will do following things:
# 1. install node module
# 2. install pods
# 3. open example.xcworkspace

npm install &&
cd example &&
npm install &&
cd ios &&
pod update &&
open PayuSdkExample.xcworkspace
